<header class='top-page-header'>
<div class='search-bar-contents'>
<%= link_to image_tag("furima-logo-color.png", class:"furima-icon"), "/" %>
<%= search_form_for @p, url: items_search_path, local: true, method: :get, class: "search-form" do |f| %>
      <%= f.label :name_eq, '商品名：' %>
      <%= f.collection_select :name_eq, @item_name, :name, :name,  include_blank: '指定なし' %>
      <br>
      <%= f.label :category_id_eq, 'カテゴリー：' %>
      <%= f.collection_select :category_id_eq, Category.all, :id, :name, include_blank: '指定なし' %>
      <br>
      
<%#= form_with(url: search_items_path, local: true, method: :get, class: "search-form") do |form| %>
  <%#= form.text_field :keyword, placeholder: 'キーワードから探す', class: 'input-box' %>
  <%#= form.submit "検索", class: "search-icon" %>
<%# end %>
</div>
<div class='nav'>
    <%= f.label :price, '価格：' %>
      <%= f.radio_button :selling_price_lteq, '' %>
      指定なし
      <%= f.radio_button :selling_price_lteq, '1000' %>
      1000円以下
      <%= f.radio_button :selling_price_lteq, '2500' %>
      2500円以下
      <%= f.radio_button :selling_price_lteq, '5000' %>
      5000円以下
      <br>
      <%= f.submit '検索', class: "search-icon"%>
    <% end %>
</header>
<div class="contents row">
<ul class='item-lists'>
  <% @results.each do |item| %>
    <%= render partial: "shared/item", locals: { item: item } %>
  <% end %>
  </ul>
</div>